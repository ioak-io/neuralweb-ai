version: '3.4'

services:
  neuralwebai:
    image: neuralwebai
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 5003:5003
    networks:
      ioak-net:
        aliases:
          - neuralwebai.docker

networks:
  ioak-net:
    external: true

